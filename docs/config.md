#### config

```go
NAME:
   mc config - Add new alias to default configuration "~/.mc/config.json" file.

USAGE:
   mc config alias NAME HOSTURL

EXAMPLES:
   1. Add alias URLs
         $ mc config alias zek https://s3.amazonaws.com/
 ```
